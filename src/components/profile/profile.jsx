import React from 'react';

import './profile.scss';

const Profile = () => {
  return (
    <div className="profile">
      {/* <h3>{`Welcome to North Shore, ${
        isAuthenticated ? user.name : `friend`
      }`}</h3> */}
      <h3>Welcome to North Shore High</h3>
    </div>
  );
};

export default Profile;
